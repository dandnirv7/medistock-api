meta {
  name: Delete
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/suppliers/{{testSupplierId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Soft delete supplier (isActive=false). Hanya ADMIN yang boleh.
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
